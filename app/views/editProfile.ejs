<!DOCTYPE html>
<html>
<head>
    <!-- Use the 'title' property to render a page title  -->
    <title><%= title %></title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css" integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">
</head>
<div class="container-fluid">
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <a class="navbar-brand" href="/">HelpingHands</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
      
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
          <ul class="navbar-nav ml-auto">
    
            <% if (title == "Home"){ %>
            <li class="nav-item active">
            <% } else  { %>
            <li class="nav-item">
            <% } %>
              <a class="nav-link" href=""><i class="fas fa-lg fa-laptop-house"></i> Home </a>
            </li>
    
            <% if (title == "About") { %>
            <li class="nav-item active">
            <% } else  { %>
            <li class="nav-item">
            <% } %>
              <a class="nav-link" href="/aboutUs"><i class="fas fa-info-circle"></i> About </a>
            </li>
            <% if (title == "Contact") { %>
            <li class="nav-item active">
            <% } else  { %>
            <li class="nav-item">
            <% } %>
              <a class="nav-link" href=""><i class="fas fa-mobile-alt"></i> Contact </a>
            </li>
        </div>
      </nav>
    <body>
          <!-- Display flash messages  -->
          <div class="alert alert-warning text-center">
            <% for(var i in badmessage) { %>
                <div class="flash"><%= badmessage[i] %></div>
             <% } %>
          </div>
        <!-- Render the signup form  -->
        <div class="container" style="width: 400px;">
            <form action="/editProfile/<%= user._id %>?_method=PUT" method="POST"" method="post">
                    <h2> Edit Profile</h2>
                        <label for="category">User Category:</label>
                        <% if ( user.category == 'Client') { %>
                            <input name="category" type="radio" value ="Provider" name="Provider">Service Provider</input>
                            <input name="category" type="radio" value ="Client" name="Client" checked="checked">Client<br />
                        <% } 
                        else { %>
                            <input name="category" type="radio" value ="Provider" name="Provider" checked="checked">Service Provider</input>
                            <input name="category" type="radio" value ="Client" name="Client">Client<br />
                        <% } %>   
                        
                        <label for="firstName">First Name:</label>
                        <input class="form-control" type="text" class ="fields" name="firstName" value="<%= user.firstName%>" />
                        <label for="lastName">Last Name:</label>
                        <input class="form-control" type="text" class ="fields" name="lastName" value="<%= user.lastName%>"/>
                        <label for="email">Email:</label>
                        <input class="form-control" type="text" class ="fields" name="email" value="<%= user.email%>"/>
                        <label for="username">Username:</label>
                        <input class="form-control" type="text" class ="fields" name="username" value="<%= user.username%>"/>
                        <label for="password">Password:</label>
                        <input required class="form-control" type="password" class ="fields" name="password" value=""/>
                        <br>
                        <button type="submit" class="btn btn-primary">Save Changes</button>
            </form>  
            <div class="text-center">
                <a href="/"> Main Page</a>
            </div>   
        </div>   
    </body>
</div>

<div>
    <footer>
      <nav class="navbar navbar-expand-lg navbar-dark bg-dark" style="position: fixed; bottom: 0; width: 98%;  margin-left: 1%; margin-right: 1%;">
        <h6 class="display-6 navbar-text">&copy; Team003-COMP231 Copyright 2021. All Rights reserved.</h6>
      </nav>
    </footer>
  </div>
</html>